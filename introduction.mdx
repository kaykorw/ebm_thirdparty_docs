---
title: Introduction
description: "Official Kayko EBM API documentation. Connect ERP and POS systems to Rwanda Revenue Authority (RRA) EBM 2.1 through the Kayko gateway."
keywords:
  - Kayko EBM API
  - RRA EBM 2.1
  - Rwanda fiscal API
  - ERP POS integration
  - electronic billing machine
icon: rocket
---

## What is Kayko EBM?

**Kayko EBM** is the API gateway between your CIS/ERP/POS application and **RRA EBM 2.1**. Your app talks only to Kayko, never to RRA directly. Kayko handles authentication, device signing, and protocol translation.

```mermaid
flowchart LR
    ERP["Your ERP / POS"] --> Kayko["Kayko EBM API"]
    Kayko --> RRA["RRA EBM 2.1"]
    RRA --> Kayko
    Kayko --> ERP
```

## Where to start

<Steps>
  <Step title="Learn the vocabulary">
    Read the [Glossary](/reference/glossary) for TIN, branch ID, item codes, tax types, and result codes.
  </Step>
  <Step title="Authenticate">
    [API Keys](/concepts/api-keys): enable EBM in the dashboard, generate a key, and send it on every request.
  </Step>
  <Step title="Follow the checklist">
    [Integration Checklist](/workflows/getting-started): recommended order from device init through codes, items, stock, sales, purchases, and imports, including how those modules connect in prose.
  </Step>
  <Step title="Build your first flow">
    Pick the API page that matches what you are building: [Sales](/api/sales), [Purchases](/api/purchases), or [Stock](/api/stock). Sales use a nested `customer` object (`tin_no`, `full_name`, `telephone`), not flat `customer_tin` fields.
  </Step>
  <Step title="Try it live">
    Switch to the **API Playground** tab to send test requests against any endpoint. Response codes are explained in [Response Codes](/reference/response-codes) (spec §4.14).
  </Step>
</Steps>

## Base URL

Code samples use `{{baseUrl}}`. On playground pages, pick the server from the **environment dropdown** (default: `{{localURL}}/ebm`).

Proxied business routes need tenant headers (not `ebm_url` in the body):

| Header | Rule |
|--------|------|
| `x-tin` | Your 9-digit TIN |
| `x-branch-id` | 2-character branch ID (`00` = head office) |
| `x-last-request-date` | 14 digits `YYYYMMDDHHmmss` on `GET /branches`, `GET /notices`, `GET /items`, `GET /item-classes`, `GET /imports`, `GET /purchases`, and `GET /stocks` |

Kayko resolves your business `ebm_url` from Core360 after you Enable EBM. You do not send it on later calls.

Item create/update/deactivate, import confirm, and purchase confirm must use head office `x-branch-id: 00`.

<Warning>
  If the EBM service loses internet connectivity for **24 hours** after issuing a receipt signature, it stops issuing new receipt numbers. Plan for offline resilience in your integration.
</Warning>

## Standard response

Many transactional upstream calls use this envelope:

```json
{
  "status": "000",
  "message": "It is succeeded",
  "date": "20260303145619",
  "data": { }
}
```

| Field | Meaning |
|-------|---------|
| `status` | `"000"` = success. Anything else → [Response Codes](/reference/response-codes). |
| `message` | Human-readable message |
| `date` | Server timestamp (`yyyyMMddHHmmss`) |
| `data` | Payload; shape varies per endpoint |

Most Kayko business routes return a mapped `{ message, data }` envelope (for example customers, branches, constants, and notices). When VSDC rejects a proxied call, Kayko remaps the failure to `{ status, message, date }` where `status` is the VSDC result code. Auth failures use `{ status, message, error }`.

## Conventions

| Convention | Value |
|------------|-------|
| Protocol | HTTPS (HTTP for local dev) |
| Method | Mix of `GET`, `POST`, `PUT`, `DELETE` (lists are `GET`) |
| Content-Type | `application/json` |
| Authentication | `Authorization: Bearer sk_…` or `x-api-key` on business routes |
