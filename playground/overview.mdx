---
title: API Playground
description: Send live test requests against Kayko EBM endpoints.
icon: play
---

Switch to the **API Playground** tab and pick an endpoint from the sidebar. Each page has a pre-filled request body, auth headers, and a **Send** button.

## Before you start

<Steps>
  <Step title="Pick an environment">
    Use the **environment dropdown** on any playground page. Default: `http://localhost:3000/ebm`. Code samples use `{{baseUrl}}` and update when you switch.
  </Step>
  <Step title="Authenticate">
    | Routes | Header |
    |--------|--------|
    | Customers, constants, sales, items, … | `Authorization: Bearer sk_…` or `x-api-key: sk_…` |
    | Initialization (`/initialize/*`) | `X-API-Token: <EBM_INTERNAL_API_TOKEN>` |

    See [API Keys](/concepts/api-keys) for business key setup.
  </Step>
  <Step title="Set ebm_url on proxied POSTs">
    Device init and customer routes need `ebm_url` in the JSON body (from Enable EBM):

    ```
    http://localhost:8080/rra/{business_code}/
    ```
  </Step>
  <Step title="Send">
    Edit the pre-filled body, click **Send**, and inspect the response panel.
  </Step>
</Steps>

## Response shapes

| Kind | Shape |
|------|-------|
| Most transactional routes | `{ resultCd, resultMsg, resultDt, data }` |
| `/customers/*` | Unwrapped `data` |
| `/constants/*` | JSON array of `{ code, sort, name, … }` |
| Auth failures | `{ statusCode, message, errorCode }` |

`resultCd: "000"` = success. Full code list: [Response Codes](/reference/response-codes).

For field-level docs and workflow guides, see the matching page under **API** in the sidebar (for example [Sales](/api/sales)).

<Note>
  The Mintlify cloud preview may not reach `localhost`. If playground calls fail, test from your machine with cURL or Postman using the same URL and payload.
</Note>

## Related

- [Integration Checklist](/workflows/getting-started)
- [API Keys](/concepts/api-keys)
